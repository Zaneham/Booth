/* Triton frontend semantic analysis, stub.
 *
 * Future sittings put the intrinsic dispatch, constexpr propagation,
 * and tile shape inference here. For now it is a holding place so
 * the build can link against the public surface. */

#include "triton.h"

int tn_sema(void)
{
    return BC_ERR_TRITON;
}
